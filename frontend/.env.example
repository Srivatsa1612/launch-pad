REACT_APP_API_URL=http://localhost:3001/api

# Admin API key (must match ADMIN_API_KEY in backend .env)
# Leave blank if auth is not enabled on the backend
# REACT_APP_ADMIN_API_KEY=your-secret-admin-key-here
